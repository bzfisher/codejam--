from sklearn import svm
dataset = sys.argv[1]

class Analyze:
	
	def putInDatabase(self, filename):
    	with open(filename, 'r') as dataset:
    		data = np.loadtxt(f)


if __name__ == '__main__':
  root = Clean()
  root.strip(dataset)
  root.load(clean_dataset)
